namespace scorepager_desktop.Structures {
	enum DrawType {
		PEN,
		HIGHLIGHTER,
		TEXT,
		SYMBOL,
		ERASER
	}

	enum SymbolType {
		NONE,
		VIOLIN_KEY,
		VALAMI
	}

	enum LayerOwner {
		USER,
		OTHER
	}
}